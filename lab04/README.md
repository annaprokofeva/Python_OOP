# ЛР-4 — Интерфейсы и абстрактные классы (ABC)

## Цель работы

Познакомиться с абстрактными базовыми классами (ABC), освоить понятие интерфейса (контракта поведения), научиться задавать обязательные методы для классов, закрепить полиморфизм через единый интерфейс.

---

## Описание интерфейсов

В файле `interfaces.py` определены два интерфейса (абстрактных класса):

| Интерфейс | Абстрактный метод | Назначение |
|-----------|-------------------|------------|
| `Printable` | `to_string() -> str` | Возвращает строковое представление объекта |
| `Comparable` | `compare_to(other) -> int` | Сравнивает текущий объект с другим |

---

## Реализация интерфейсов в классах

Классы `Student`, `BachelorStudent`, `MasterStudent` и `PhDStudent` из файла `models.py` наследуют оба интерфейса и реализуют их методы:

- **`to_string()`** – вызывает `__str__`, который для каждого типа формирует строку с учётом специфических атрибутов
- **`compare_to()`** – сравнивает двух студентов по среднему баллу

**Валидация импортируется из `lab01/validate.py`**

---

## Демонстрация (3 сценария)

### Сценарий 1: Вызов методов интерфейса

Создаются объекты трёх типов: бакалавр, магистр, аспирант. Для каждого вызывается `to_string()` через интерфейс `Printable`. Вывод показывает различное строковое представление – полиморфизм на практике.

![1](/Python_OOP/pic/4_1.png)
![1](/Python_OOP/pic/4_1_1.png)

### Сценарий 2: Полиморфная функция через интерфейс

Функция `print_all_printable()` принимает список объектов, реализующих `Printable`, и печатает их. Также проверяется реализация интерфейсов через `isinstance()`.

![1](/Python_OOP/pic/4_2.png)
![1](/Python_OOP/pic/4_2_2.png)


### Сценарий 3: Коллекция и фильтрация

Создаётся коллекция `StudentCollection` (встроена в `demo.py`). В неё добавляются объекты разных типов. Затем:
- Вывод исходной коллекции
- Фильтрация по интерфейсу `Printable`
- Сортировка по среднему баллу
- Сравнение двух студентов через `compare_to()`

![1](/Python_OOP/pic/4_3.png)

---

## Структура проекта
